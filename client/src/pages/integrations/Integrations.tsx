// import classes from "./Integrations.module.css";

const Integrations = () => {
  return (
    <div>
      <div>Include API Keys, webhook, option to create contract, etc...</div>
    </div>
  );
};

export default Integrations;
