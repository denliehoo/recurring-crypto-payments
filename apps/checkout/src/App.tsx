import { testMonorepo } from "@components";

const App = () => {
  return (
    <div>
      <button onClick={() => testMonorepo()}>Test</button>
      Hello World!!
    </div>
  );
};

export default App;
